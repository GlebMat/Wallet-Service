version: '3'
services:
  postgres:
    image: postgres:15.4
    container_name: my_postgres
    environment:
      POSTGRES_USER: wallet
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: mydb
      POSTGRES_INITDB_ARGS: "--auth-host=md5 --auth-local=md5 -U wallet"
    ports:
      - "5432:5432"
